<fieldset class="albumlist">
	<legend>Photos already in this Photobook</legend>
	
	<% @photos.each do |photo| -%>
	
		<%= link_to image_tag(photo.public_filename(:thumb)), :action => 'photo', :id => photo.id %>
	
	<% end -%>
	
</fieldset>
